extends layout

block content
  .main.container
    .row.justify-content-center
      .col-md-6.col-md-offset-3
        img(src='/images/retroctf-logo.png' alt='Retro CTF')
    .row.justify-content-center
      div.col-md-6.col-md-offset-3
        p RetroCTF is a online capture the flag (CTF) wargame in which challenges consist solely of 8-bit and 16-bit game ROM's. RetroCTF is intended to be played as an individual, at your own pace.
        h1.display-4.m-b-2 Sign In
        span Don't have an account? 
          a(href='/signup') Sign Up
        form(method='POST' action='/login')
          div.form-group
            input.form-control(type='text' id='email' placeholder='Email' name='email' maxlength='128')
          div.form-group
            input.form-control(type='password' id='password' placeholder='Password' name='password')
          p.text-danger #{status}
          button.btn.btn-primary(type='submit') Log in
